{
  "name": "dotfiles-workspace",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": {
    "packages": [
      "dotfiles",
      "packages/chatgpt"
    ],
    "catalog": {
      "@types/bun": "1.3.14",
      "typescript": "7.0.2"
    }
  },
  "scripts": {
    "check": "bun run check:biome && bun x --no-install tsc -p tsconfig.json && bun run --sequential --workspaces check && bun run --cwd packages/hyper-window-tiling check",
    "check:biome": "bun x --no-install biome check .",
    "format": "bun x --no-install biome check --write .",
    "lint": "bun x --no-install biome lint .",
    "lint:fix": "bun x --no-install biome lint --write .",
    "typecheck": "bun x --no-install tsc -p tsconfig.json && bun run --sequential --workspaces typecheck && bun run --cwd packages/hyper-window-tiling typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
